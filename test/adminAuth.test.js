const request = require("supertest");
describe("login", () => {
    it("should get admin from database", async() => {

    })
    it("compare password should be true", async () => {

    })
    it("should genrate jwt", async () => {
        
    })
})

describe("signup", () => {
    it("should hash password", async () => {
        
    })
    it("should save user into data base", async () => {
        
    })
})